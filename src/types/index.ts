export interface TextoLabel {
  id: string;
  texto: string;
  latitud: number;
  longitud: number;
  fontSize: number;
  color: string;
  rotacion: number;
  creado_at: string;
}

export interface BusquedaResultado {
  // ... el resto no cambia
}export interface TextoLabel {
  id: string;
  texto: string;
  latitud: number;
  longitud: number;
  fontSize: number;
  color: string;
  rotacion: number;
  creado_at: string;
}

export interface BusquedaResultado {
  // ... el resto no cambia
}
